interface:
  display_name: "Operit"
  short_description: "Repo-specific patterns and workflows for Operit"
  default_prompt: "Use the Operit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true