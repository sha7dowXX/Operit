# Manifest API 版本与兼容性

## 旧实现

manifest 只声明包元数据和资源结构，宿主无法知道包使用了哪一版 ToolPkg API。

## 目标

- 增加 `api_version`。
- 缺少该字段的 manifest 使用 `1.0.0`。
- `1.12.1+4` 支持 `1.0.0` 和 `1.0.1`。
- 不支持的 API 版本在包进入执行阶段前明确报告包名、声明版本和宿主支持版本。

[DONE]
