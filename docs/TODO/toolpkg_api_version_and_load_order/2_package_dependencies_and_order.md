# 包依赖与加载顺序

## 旧实现

ToolPkg 容器按包名排序加载，没有 manifest 级别的必需包和加载顺序约束。

## 目标

- `requires` 声明必须存在并启用的依赖对象，同时决定依赖包先于声明方加载。
- 每项依赖包含 `id`、面向用户的 `description`，以及可选的 `min_version` / `max_version`。
- 依赖不存在、未启用、重复声明或形成环时，当前包不进入执行阶段并报告原因。
- 没有关系约束时使用插件列表中的用户顺序，最后以包 ID 保持确定性。

[DONE]
