# 存储写入与错误提示

## 旧实现

工作流主文件使用普通文本写入。若写入过程中进程中断，下一次读取可能遇到残缺 JSON。多个仓库实例也可能同时读写同一目录，造成列表结果不稳定。

格式错误的工作流文件在批量加载时仅被跳过，用户很难知道是存储文件出了问题。

## 修正意图

用 `AtomicFile` 提交工作流和运行记录 JSON，仓库层用共享互斥锁保护工作流主文件读写。读取列表时聚合格式错误文件名，通过可见提示告知用户哪些文件被跳过。

## 影响范围

- 新增工作流存储异常文案
- 工作流主文件读写、删除和批量加载
- 工作流运行记录写入

[DONE]
