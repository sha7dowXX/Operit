# UI 加载时序

## 旧实现

工作流更新事件会再次调用公开加载方法并启动新的协程。用户快速进入详情、返回列表或连续操作时，较早请求可能晚于较新请求完成，从而覆盖当前界面状态。

## 修正意图

列表加载和详情加载都分配递增请求编号，只有当前最新请求可以写入列表、当前工作流、最近执行记录或错误状态。

错误展示改为与包管理界面一致的右下角 snackbar，避免格式错误提示被静默吞掉。

## 影响范围

- `WorkflowViewModel` 列表和详情加载入口
- 工作流列表页错误展示
- 工作流详情页错误展示

[DONE]
